# values.yaml — Domaine des Rêves Bleus (prod)
# Les images sont mises à jour automatiquement par la CI (build-and-push.yml)
# Format : ghcr.io/<owner>/<nom>@sha256:<digest>  (piqué par la CI)

namespace: domaine-des-reves-bleus

# ── Secret Kubernetes (déployé via SealedSecret dans templates/) ──────────────
secretName: domaine-des-reves-bleus-secrets

# ── Backend (Node.js) ─────────────────────────────────────────────────────────
backend:
  image: ghcr.io/emmguardia/domaine-des-reves-bleus-back@sha256:4f7f24b52c14539a785ad3987a7722a9ffa6c94c65972d16694c144aee1bca23
  imagePullPolicy: IfNotPresent
  replicas: 1
  strategy:
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  port: 8000
  env:
    NODE_ENV: production
    PORT: "8000"
    FRONTEND_URL: "https://domainedesrevesbleus.eu"
    EMAIL_SERVICE_URL: "http://email-service.email-service.svc.cluster.local:8080"
    ADMIN_EMAIL: "domainedesrevesbleus@gmail.com"
    MARIADB_PORT: "3306"
    JWT_PRIVATE_KEY_PATH: "/app/secrets/jwt_private_key.pem"
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    cpuTarget: 70
    memoryTarget: 80
  metrics:
    # Mettre à true si Prometheus Operator est installé (kube-prometheus-stack)
    # pour activer le ServiceMonitor de scraping automatique.
    # L'endpoint /api/metrics est toujours exposé, indépendamment de ce flag.
    enabled: false
    scrapeInterval: 30s

# ── Frontend (nginx) ──────────────────────────────────────────────────────────
frontend:
  image: ghcr.io/emmguardia/domaine-des-reves-bleus-front@sha256:29fa7fbd5386fbd33a31e7de23b2bd1241fbe4a5941a8eeb6de1232793119738
  imagePullPolicy: IfNotPresent
  replicas: 1
  strategy:
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  port: 8080
  resources:
    requests:
      cpu: "50m"
      memory: "64Mi"
    limits:
      cpu: "200m"
      memory: "128Mi"
  hpa:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    cpuTarget: 70
    memoryTarget: 80
