// Google Analytics désactivé - fonctions vides pour éviter les erreurs
export const trackEvent = (_eventName: string, _eventParams?: Record<string, any>): void => {
  // Fonction désactivée - ne fait rien
};

export const trackPageView = (_path: string): void => {
  // Fonction désactivée - ne fait rien
};

